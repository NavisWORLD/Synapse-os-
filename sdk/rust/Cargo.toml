[package]
name = "synapse-os-sdk"
version = "0.2.0"
edition = "2021"
license-file = "../../LICENSE"
description = "Safe Rust bindings for the stable Synapse OS C ABI"

[lib]
name = "synapse_os_sdk"
path = "src/lib.rs"
