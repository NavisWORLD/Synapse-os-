[package]
name = "synapse-os-sdk"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Zero-dependency Rust access to Synapse OS telemetry"

[lib]
name = "synapse_os_sdk"
path = "src/lib.rs"
