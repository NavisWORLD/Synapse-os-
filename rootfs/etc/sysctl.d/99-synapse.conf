# Synapse OS conservative workstation defaults.
# Prefer compressed RAM swap over slow disk swap when zram is active.
vm.swappiness = 80
vm.page-cluster = 0
# Developer-heavy workloads can exhaust default inotify watcher counts.
fs.inotify.max_user_watches = 1048576
fs.inotify.max_user_instances = 1024
