# Reference layout. apple_efi_install.sh renders the real UUID/kernel names.
set timeout=5
set default=0
menuentry 'Synapse OS // Apple Intel' {
  search --no-floppy --label --set=root SYNAPSE_ROOT
  echo 'Synapse Apple Intel fallback entry; use apple_efi_install.sh to render exact kernel paths.'
}
