# Phone controls

The canonical phone control surfaces are copied into this folder from:

- `phone-bootstrap/FLASH_USB.html`
- `phone-bootstrap/GENESIS.html`

The complete Synapse ISO also carries the GENESIS phone-control assets, so the installer remains self-contained after boot.
